# 达摩盘 v2.3.59 官网黄金基线约束

本文件覆盖本官网仓库。达摩盘 `v2.3.59` 是用户确认的黄金基线和永久回滚版本。

## 永久保留

- 私有安装包 `private-assets/dmp/shaozhuang-dmp-unified-automation-v2.3.59.zip`
- ZIP SHA-256 `98abed5ddad3f6dd12d46d7c8d758134a0e4b699bcf53406b79c3d6eed99be27`
- 官网完整性引擎 SHA-256 `21d2d0a1b554e54530e06c21e70b2680a7bab411601c3e827e0ce0745cee29c0`
- 官网报告引擎 SHA-256 `6495010735c43c792864c13c85146d3542aa75e365b3afaa12dbb74bed1beffa`

任何后续版本都不得覆盖或删除该 ZIP；发布保护必须继续校验其哈希。

## 规则变更提醒

修改 DMP 下载版本、受保护 ZIP、公共报告引擎、报告导入/格式化/Viewer、归档 API、店铺路由、默认同步、自动打开、同 ID 原子替换或报告存储前，必须先向用户说明旧行为、新行为、影响范围和回滚方式，并在当前任务中取得明确确认。即使请求仅是“增加参数”，只要可能改变 v2.3.59 的旧输入、输出或副作用，也必须先提示。

未确认时只允许只读审计和运行测试。确认后必须发新版本；不得回写 v2.3.59。发布前后必须运行完整发布保护，并保留上一生产目录和 Nginx 秒级回滚入口。
